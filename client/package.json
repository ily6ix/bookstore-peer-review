{
  "name": "desktop",
  "version": "1.0.0",
  "description": "**Overview** This prototype contains: - Android app (Kotlin) for on-device human-presence inference using Wi‑Fi / Bluetooth RSSI and IMU features. - Python scripts for synthetic data simulation, model training (produces a `model.tflite`), and evaluation. - A privacy policy and README with quick run instructions.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.1"
  },
  "devDependencies": {}
}
